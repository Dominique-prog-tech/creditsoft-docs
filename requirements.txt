# De handleiding-bouw, met vastgelegde versies.
#
# ⚠️ WAAROM DIT BESTAND ER IS (26/08/2026). `deploy.yml` deed hiervoor:
#     pip install mkdocs-material mkdocs-material-extensions mkdocs-static-i18n mkdocs-glightbox
# zonder één versienummer. Elke bouw haalde dus binnen wat er die dag nieuw stond.
#
# Dat is geen theoretisch risico: `mkdocs build --strict` waarschuwt dat **MkDocs 2.0 het plugin-systeem
# schrapt en het theming-systeem herschrijft**. Deze handleiding steunt volledig op plugins —
# `mkdocs-static-i18n` IS de tweetaligheid. Zonder pin zou de eerstvolgende push ná die release de NL/FR-opbouw
# stilleggen, bij een commit die er niets mee te maken heeft.
#
# Opwaarderen doe je dus BEWUST: versie omhoog, `mkdocs build --strict` lokaal draaien, de Franse bovenbalk
# nakijken, pas dan committen. Niet blind `pip install -U`.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
mkdocs-static-i18n==1.3.1
mkdocs-glightbox==0.5.2
